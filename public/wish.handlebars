<div class="container">
  <div class="row">
    <div class="wishlist col-md-6 col-md-offset-3">

      <input type="radio" name="wish" value="{{ id }}" />  <strong>{{ item_name }}</strong> - {{ item_url }} - ${{ estimated_price }}

    </div>
  </div>
</div>
