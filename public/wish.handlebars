<div class="container">

  <div class="row">
    <div class="col-md-3">
      <input type="radio" name="wish" value="{{ id }}" /> <strong>{{ item_name }}</strong>
    </div>

    <div class="wishlist col-md-3">
      {{ item_url }}
    </div>

    <div class="wishlist col-md-3">
       ${{ estimated_price }}
    </div>

  </div>

</div>
